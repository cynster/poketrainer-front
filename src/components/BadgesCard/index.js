import { Card, Form, Button, Col } from "react-bootstrap";
import { useNavigate, Link } from "react-router-dom";

export default function BadgesCard() {
  return (
        <Card.Body>
          <Card.Text>Badges BadgesCard</Card.Text>
        </Card.Body>
  );
}
